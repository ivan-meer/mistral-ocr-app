# Обязательно, если USE_MOCK_OCR=false. Замените на свой ключ Mistral AI.
MISTRAL_API_KEY=your-key

# Использовать демо-режим OCR (true/false). Если true, API-ключ не требуется.
# В демо-режиме реальные запросы к API Mistral не выполняются.
USE_MOCK_OCR=true

# Максимальный размер загружаемого файла в мегабайтах (МБ).
# Например, 50 для 50МБ.
MAX_FILE_SIZE_MB=50

# Папка для временных файлов и результатов OCR.
# Если значение закомментировано или отсутствует, будет использоваться системная временная папка.
# Убедитесь, что у процесса Flask есть права на запись в эту папку.
# Пример для Linux/macOS: UPLOAD_FOLDER=/tmp/mistral_ocr_uploads
# Пример для Windows: UPLOAD_FOLDER=C:\temp\mistral_ocr_uploads
# UPLOAD_FOLDER=

# Настройки хоста и порта для Flask-сервера.
# HOST=0.0.0.0 означает, что сервер будет доступен со всех сетевых интерфейсов.
HOST=0.0.0.0
PORT=5000

# Режим отладки Flask (true/false).
# В production-среде рекомендуется установить значение false для безопасности и производительности.
DEBUG=True
